//用户库部分

包含文件：DBSecurity.h  #用户信息加密算法，将用户密码加密后保存以确保用户信息安全性（使用了MD5算法和仿RAR的加密算法）
             /  DBStruct.h      #定义了用户信息储存的基本结构（字段）
             /  DBUAdmin.h   #数据库的最后封装，把整个数据库封作整体

< 注意事项 >

# 又是历史遗留问题，User中的主键UserID是一个int值，而在Book中写的是char*，所以要转换后使用